{
  "name": "@pulse-brazil/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Pure domain layer for Pulse Brazil: entities, value objects, and invariants. No framework, infrastructure, or UI dependencies.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "dependencies": {}
}
